# Holiday Email Orchestrator - Frontend Environment Configuration
# 
# Copy this file to .env.local and fill in your values:
#   cp .env.local.example .env.local
#
# The .env.local file is gitignored and will not be committed.

# =============================================================================
# WEBHOOK URL (Required)
# =============================================================================
# The n8n webhook URL that handles holiday email generation and sending.
#
# Local development (n8n running on your machine):
#   VITE_WEBHOOK_URL=http://localhost:5678/webhook/your-webhook-id
#
# Render-hosted n8n:
#   VITE_WEBHOOK_URL=https://your-n8n-service.onrender.com/webhook/your-webhook-id
#
# ngrok tunnel to local n8n:
#   VITE_WEBHOOK_URL=https://abc123.ngrok.io/webhook/your-webhook-id

VITE_WEBHOOK_URL=http://localhost:5678/webhook/REPLACE_ME

# =============================================================================
# API BASE URL (Required for logging)
# =============================================================================
# The backend API URL for logging email batches to Postgres.
#
# Local development:
#   VITE_API_BASE_URL=http://localhost:4000
#
# Production (Render-hosted backend):
#   VITE_API_BASE_URL=https://your-backend.onrender.com

VITE_API_BASE_URL=http://localhost:4000

# =============================================================================
# SECURITY (Required)
# =============================================================================

# API Key for authenticating with the backend.
# MUST match API_KEY_FRONTEND in your backend .env file.
# Generate with: node -e "console.log(require('crypto').randomBytes(24).toString('hex'))"
VITE_API_KEY_FRONTEND=your-api-key-here

# Secret header sent with n8n webhook requests.
# MUST match N8N_WEBHOOK_SECRET in your n8n workflow.
# Generate with: node -e "console.log(require('crypto').randomBytes(24).toString('hex'))"
VITE_N8N_SECRET=your-n8n-secret-here

# =============================================================================
# OPTIONAL
# =============================================================================

# Password hint displayed on the login page (optional).
# Example: "Contact admin for access" or "Check your onboarding email"
# VITE_ACCESS_PASSWORD_HINT=Contact your administrator for access
